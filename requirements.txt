# Talks to the pad over USB HID (e.g. NuPhy Air75 V2, Keychron K1 Pro).
# Also requires the native hidapi library (e.g. `brew install hidapi`
# on macOS). Without it, daemon.py still runs — it just stays headless,
# logging what it would send instead of writing to a pad.
hid
